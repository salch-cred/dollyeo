(()=>{const $=(s,p=document)=>p.querySelector(s),$$=(s,p=document)=>[...p.querySelectorAll(s)];const reduced=matchMedia('(prefers-reduced-motion: reduce)').matches;
// Scroll progress
const progress=document.createElement('div');progress.className='scroll-progress';document.body.appendChild(progress);const updateScroll=()=>{const max=document.documentElement.scrollHeight-innerHeight;progress.style.setProperty('--scroll',`${max>0?scrollY/max*100:0}%`)};addEventListener('scroll',updateScroll,{passive:true});updateScroll();
// Reveal sections once, with a spring entrance.
const revealTargets=$$('.section-head,.proof-bar,.steps-track,.threat-console,.network-benefits,.principle-copy,.principle-stack,.developer-section,.final-cta>div,.metrics-grid,.dashboard-grid,.safety-strip,.form-card,.case-board,.registry-search,.registry-stats,.registry-table,.console-grid');revealTargets.forEach(el=>el.classList.add('motion-reveal'));if(reduced)revealTargets.forEach(el=>el.classList.add('is-visible'));else{const io=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('is-visible');io.unobserve(e.target)}}),{threshold:.08,rootMargin:'0px 0px -35px'});revealTargets.forEach(el=>io.observe(el))}
// Pointer-refraction glare and restrained 3D tilt.
const cards=$$('.core-card,.step-card,.metric,.panel,.form-card,.large-case,.registry-stats>div,.registry-table,.console-form,.policy-card,.network-benefits article,.principle-stack article,.story-card,.modal-card');cards.forEach(card=>{card.classList.add('motion-card');if(reduced)return;card.addEventListener('pointermove',e=>{if(innerWidth<820)return;const r=card.getBoundingClientRect(),x=(e.clientX-r.left)/r.width,y=(e.clientY-r.top)/r.height;card.style.setProperty('--glare-x',`${x*100}%`);card.style.setProperty('--glare-y',`${y*100}%`);const rx=(.5-y)*3.8,ry=(x-.5)*4.8;card.style.transform=`perspective(1000px) rotateX(${rx}deg) rotateY(${ry}deg) translateY(-3px)`});card.addEventListener('pointerleave',()=>{card.style.transform='';card.style.removeProperty('--glare-x');card.style.removeProperty('--glare-y')})});
// Magnetic controls.
$$('.pill-button,.primary-btn,.ghost-button,.secondary-btn').forEach(btn=>{if(!reduced){btn.addEventListener('pointermove',e=>{const r=btn.getBoundingClientRect();btn.style.transform=`translate(${(e.clientX-r.left-r.width/2)*.09}px,${(e.clientY-r.top-r.height/2)*.12}px)`});btn.addEventListener('pointerleave',()=>btn.style.transform='')};btn.addEventListener('click',e=>{const r=btn.getBoundingClientRect(),dot=document.createElement('span'),size=Math.max(r.width,r.height);dot.className='ripple';dot.style.width=dot.style.height=`${size}px`;dot.style.left=`${e.clientX-r.left-size/2}px`;dot.style.top=`${e.clientY-r.top-size/2}px`;btn.appendChild(dot);setTimeout(()=>dot.remove(),650)})});
// Gentle parallax on decorative hero cards.
if(!reduced){addEventListener('pointermove',e=>{if(innerWidth<820)return;const nx=e.clientX/innerWidth-.5,ny=e.clientY/innerHeight-.5;$$('.float-card,.float-chip').forEach((el,i)=>{const d=8+(i%3)*4;el.style.translate=`${nx*d}px ${ny*d}px`})},{passive:true})}
// Pause expensive animation when the tab is hidden.
document.addEventListener('visibilitychange',()=>document.documentElement.classList.toggle('motion-paused',document.hidden));
})();
