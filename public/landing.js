const $=(s,p=document)=>p.querySelector(s),$$=(s,p=document)=>[...p.querySelectorAll(s)];
// Scroll reveals
const observer=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('revealed');observer.unobserve(e.target)}}),{threshold:.13});$$('[data-reveal]').forEach(el=>observer.observe(el));
// Animated counters
const counterObserver=new IntersectionObserver(entries=>entries.forEach(e=>{if(!e.isIntersecting)return;const el=e.target,target=+el.dataset.count,start=performance.now(),duration=1300;function tick(now){const p=Math.min((now-start)/duration,1),v=Math.round(target*(1-Math.pow(1-p,3)));el.textContent=v+(target===8?'m':'');if(p<1)requestAnimationFrame(tick)}requestAnimationFrame(tick);counterObserver.unobserve(el)}),{threshold:.8});$$('[data-count]').forEach(el=>counterObserver.observe(el));
// Aurora canvas
const canvas=$('#aurora'),ctx=canvas.getContext('2d');let W,H,t=0;function resize(){W=canvas.width=innerWidth*devicePixelRatio;H=canvas.height=innerHeight*devicePixelRatio;canvas.style.width=innerWidth+'px';canvas.style.height=innerHeight+'px'}resize();addEventListener('resize',resize);function aurora(){t+=.003;ctx.clearRect(0,0,W,H);const blobs=[[.18,.1,'#39e4a5',.18],[.76,.18,'#416dff',.12],[.55,.75,'#9c55ff',.07]];blobs.forEach((b,i)=>{const x=(b[0]+Math.sin(t*(i+1))*.06)*W,y=(b[1]+Math.cos(t*(i+1.3))*.05)*H,r=Math.max(W,H)*.42,g=ctx.createRadialGradient(x,y,0,x,y,r);g.addColorStop(0,b[2]+Math.round(b[3]*255).toString(16).padStart(2,'0'));g.addColorStop(1,b[2]+'00');ctx.fillStyle=g;ctx.fillRect(0,0,W,H)});requestAnimationFrame(aurora)}if(!matchMedia('(prefers-reduced-motion: reduce)').matches)aurora();
// Pointer glow and magnetic interactions
addEventListener('pointermove',e=>{document.documentElement.style.setProperty('--mx',e.clientX+'px');document.documentElement.style.setProperty('--my',e.clientY+'px')});$$('.magnetic').forEach(el=>{el.addEventListener('pointermove',e=>{const r=el.getBoundingClientRect();el.style.transform=`translate(${(e.clientX-r.left-r.width/2)*.12}px,${(e.clientY-r.top-r.height/2)*.12}px)`});el.addEventListener('pointerleave',()=>el.style.transform='')});
$$('.tilt-card').forEach(el=>{el.addEventListener('pointermove',e=>{const r=el.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;el.style.transform=`perspective(900px) rotateX(${-y*5}deg) rotateY(${x*7}deg) translateZ(8px)`});el.addEventListener('pointerleave',()=>el.style.transform='')});
// Header/mobile
const header=$('.site-header');addEventListener('scroll',()=>header.classList.toggle('scrolled',scrollY>40));$('.menu').onclick=()=>header.classList.toggle('open');$$('.site-header nav a').forEach(a=>a.onclick=()=>header.classList.remove('open'));
// Story modal
const modal=$('.story-modal'),status=$('.story-status'),progress=$('.story-progress i'),run=$('#runStory');$('#storyButton').onclick=()=>{modal.classList.add('open');modal.setAttribute('aria-hidden','false')};$('.story-close').onclick=close;modal.onclick=e=>{if(e.target===modal)close()};function close(){modal.classList.remove('open');modal.setAttribute('aria-hidden','true');progress.style.width='0'}
run.onclick=()=>{const scenes=[['Transfer detected','hgi-alert-02'],['Issuer verified','hgi-certificate-01'],['Settlement paused','hgi-lock-01'],['Funds returned','hgi-checkmark-circle-02']];let i=0;run.disabled=true;const visual=$('.story-shield i');const timer=setInterval(()=>{status.textContent=scenes[i][0];visual.className=`hgi hgi-stroke ${scenes[i][1]}`;progress.style.width=((i+1)/scenes.length*100)+'%';if(++i===scenes.length){clearInterval(timer);run.disabled=false;run.innerHTML='<i class="hgi hgi-stroke hgi-refresh"></i> Replay story'}},950)};
addEventListener('keydown',e=>{if(e.key==='Escape')close()});
